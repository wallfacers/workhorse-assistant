# i18n 国际化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Workhorse Assistant 前端添加 react-i18next 国际化支持，覆盖 zh-CN 和 en-US 两种语言，所有 21 个组件一次性完成改造。

**Architecture:** 使用 react-i18next + i18next-browser-languagedetector 建立基础设施。翻译 JSON 按组件分组嵌套，以 zh-CN 为基准语言。语言检测优先 localStorage → navigator.language → 回退 zh-CN。SettingsModal 中新增语言选择器。

**Tech Stack:** React 19, TypeScript, i18next, react-i18next, i18next-browser-languagedetector

---

## Task 1: 安装依赖 + 创建 i18n 基础设施

**Files:**
- Create: `src/i18n/index.ts`
- Modify: `src/main.tsx:1-2`

- [ ] **Step 1: 安装 i18n 依赖**

```bash
npm install i18next react-i18next i18next-browser-languagedetector
```

Run: `npm install i18next react-i18next i18next-browser-languagedetector`
Expected: 依赖安装成功，package.json 更新

- [ ] **Step 2: 创建 i18n 初始化文件**

```typescript
// src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import zhCN from './locales/zh-CN.json';
import enUS from './locales/en-US.json';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      'zh-CN': { translation: zhCN },
      'en-US': { translation: enUS },
    },
    fallbackLng: 'zh-CN',
    interpolation: { escapeValue: false },
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: 'workhorse:locale',
      caches: ['localStorage'],
    },
  });

export default i18n;
```

- [ ] **Step 3: 在 main.tsx 中导入 i18n（在 createRoot 之前）**

```typescript
// src/main.tsx — 在第 1 行之后添加 i18n import
import {StrictMode} from 'react';
import {createRoot} from 'react-dom/client';
import './i18n';           // ← 新增：必须在 App 之前初始化
import App from './App.tsx';
import './index.css';
```

- [ ] **Step 4: 验证 TypeScript 编译通过**

Run: `npm run lint`
Expected: PASS（此时还没有 JSON 文件，下一步创建）

---

## Task 2: 创建中文翻译文件 (zh-CN.json)

**Files:**
- Create: `src/i18n/locales/zh-CN.json`

- [ ] **Step 1: 创建完整的中文翻译 JSON**

```json
{
  "common": {
    "allow": "允许",
    "deny": "拒绝",
    "close": "关闭",
    "retry": "重试",
    "copy": "复制",
    "send": "发送",
    "stop": "停止",
    "confirm": "确认",
    "cancel": "取消",
    "open": "打开"
  },
  "agent": {
    "placeholder": "请输入任务，交给我来完成",
    "welcome": "有什么可以帮你的？",
    "generating": "正在生成",
    "chooseFile": "选择文件",
    "scrollToBottom": "滚到底部",
    "permissionRequest": "请求权限：",
    "sensitiveAction": "敏感操作",
    "allowedRemembered": "已允许（本次会话记住）",
    "denied": "已拒绝",
    "openTaskList": "打开任务列表",
    "openSettings": "打开设置",
    "status": {
      "disconnected": "未连接",
      "connecting": "连接中…",
      "connected": "已连接（session {{sessionId}}）",
      "failed": "连接失败：{{error}}"
    },
    "feedback": {
      "copy": "复制",
      "good": "好用",
      "bad": "不好"
    }
  },
  "settings": {
    "title": "设置",
    "closeSettings": "关闭设置",
    "nav": {
      "theme": "主题",
      "shortcuts": "快捷键",
      "agent": "Agent"
    },
    "appearance": "外观",
    "lightTheme": "浅色",
    "darkTheme": "深色",
    "keyboardShortcuts": "键盘快捷键",
    "connection": "连接",
    "endpoint": "服务地址",
    "defaultValue": "默认值",
    "endpointHint": "可通过 WORKHORSE_AGENT_ENDPOINT 环境变量修改",
    "disconnect": "断开连接",
    "reconnect": "重新连接",
    "reasoningDisplay": "推理显示",
    "autoExpandThinking": "自动展开思考过程",
    "autoExpandDescription": "流式推理时自动展开，完成后自动折叠",
    "language": "语言"
  },
  "shortcuts": {
    "newTask": "新建任务",
    "globalSearch": "全局搜索",
    "openSettings": "打开设置",
    "closePanel": "关闭面板",
    "toggleSidebar": "切换侧栏",
    "newTerminal": "新建终端",
    "splitTerminal": "分割终端",
    "sendMessage": "发送消息",
    "showShortcuts": "显示快捷键",
    "cancelClose": "取消 / 关闭"
  },
  "tasks": {
    "title": "任务列表",
    "closeTaskList": "关闭任务列表",
    "searchPlaceholder": "搜索任务…",
    "noMatch": "无匹配任务"
  },
  "workspace": {
    "collapsePanel": "收起工作台",
    "expandPanel": "展开工作台",
    "tabs": {
      "directory": "目录",
      "info": "信息",
      "preview": "预览"
    },
    "fields": {
      "accessId": "访问 ID",
      "tags": "标签",
      "createdAt": "创建时间",
      "branch": "分支",
      "originalCommand": "原始指令"
    },
    "actions": {
      "open": "打开",
      "revealInFolder": "所在位置"
    }
  },
  "terminal": {
    "noTerminal": "暂无终端",
    "newTerminal": "新建终端",
    "newGroup": "新建终端组",
    "closeGroup": "关闭终端组",
    "close": "关闭"
  },
  "toolCall": {
    "running": "运行中…",
    "done": "完成",
    "error": "出错",
    "input": "输入",
    "output": "输出"
  },
  "error": {
    "boundaryTitle": "这个区域出了点问题",
    "unknown": "未知渲染错误"
  },
  "reasoning": {
    "thinking": "思考中…",
    "redacted": "已隐藏的推理",
    "duration": "深度思考 {{seconds}} 秒",
    "redactedMessage": "此推理块已加密（redacted），内容不可见",
    "default": "深度思考"
  },
  "codeBlock": {
    "copy": "复制代码"
  },
  "mainChat": {
    "conversationTitle": "与Workhorse的对话",
    "placeholder": "请输入任务，交给我来帮你完成",
    "aiDisclaimer": "以上内容由 AI 生成"
  }
}
```

- [ ] **Step 2: 验证 JSON 有效**

Run: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/zh-CN.json','utf8')); console.log('valid')"`
Expected: `valid`

---

## Task 3: 创建英文翻译文件 (en-US.json)

**Files:**
- Create: `src/i18n/locales/en-US.json`

- [ ] **Step 1: 创建完整的英文翻译 JSON**

```json
{
  "common": {
    "allow": "Allow",
    "deny": "Deny",
    "close": "Close",
    "retry": "Retry",
    "copy": "Copy",
    "send": "Send",
    "stop": "Stop",
    "confirm": "Confirm",
    "cancel": "Cancel",
    "open": "Open"
  },
  "agent": {
    "placeholder": "Enter a task, I'll handle it",
    "welcome": "How can I help you?",
    "generating": "Generating",
    "chooseFile": "Choose file",
    "scrollToBottom": "Scroll to bottom",
    "permissionRequest": "Permission request: ",
    "sensitiveAction": "Sensitive action",
    "allowedRemembered": "Allowed (remembered for this session)",
    "denied": "Denied",
    "openTaskList": "Open task list",
    "openSettings": "Open settings",
    "status": {
      "disconnected": "Disconnected",
      "connecting": "Connecting…",
      "connected": "Connected (session {{sessionId}})",
      "failed": "Connection failed: {{error}}"
    },
    "feedback": {
      "copy": "Copy",
      "good": "Good",
      "bad": "Bad"
    }
  },
  "settings": {
    "title": "Settings",
    "closeSettings": "Close settings",
    "nav": {
      "theme": "Theme",
      "shortcuts": "Shortcuts",
      "agent": "Agent"
    },
    "appearance": "Appearance",
    "lightTheme": "Light",
    "darkTheme": "Dark",
    "keyboardShortcuts": "Keyboard Shortcuts",
    "connection": "Connection",
    "endpoint": "Endpoint",
    "defaultValue": "Default",
    "endpointHint": "Can be changed via WORKHORSE_AGENT_ENDPOINT env var",
    "disconnect": "Disconnect",
    "reconnect": "Reconnect",
    "reasoningDisplay": "Reasoning Display",
    "autoExpandThinking": "Auto-expand thinking process",
    "autoExpandDescription": "Auto-expand during streaming, auto-collapse when done",
    "language": "Language"
  },
  "shortcuts": {
    "newTask": "New task",
    "globalSearch": "Global search",
    "openSettings": "Open settings",
    "closePanel": "Close panel",
    "toggleSidebar": "Toggle sidebar",
    "newTerminal": "New terminal",
    "splitTerminal": "Split terminal",
    "sendMessage": "Send message",
    "showShortcuts": "Show shortcuts",
    "cancelClose": "Cancel / Close"
  },
  "tasks": {
    "title": "Task List",
    "closeTaskList": "Close task list",
    "searchPlaceholder": "Search tasks…",
    "noMatch": "No matching tasks"
  },
  "workspace": {
    "collapsePanel": "Collapse panel",
    "expandPanel": "Expand panel",
    "tabs": {
      "directory": "Directory",
      "info": "Info",
      "preview": "Preview"
    },
    "fields": {
      "accessId": "Access ID",
      "tags": "Tags",
      "createdAt": "Created",
      "branch": "Branch",
      "originalCommand": "Original command"
    },
    "actions": {
      "open": "Open",
      "revealInFolder": "Reveal in folder"
    }
  },
  "terminal": {
    "noTerminal": "No terminals",
    "newTerminal": "New terminal",
    "newGroup": "New terminal group",
    "closeGroup": "Close terminal group",
    "close": "Close"
  },
  "toolCall": {
    "running": "Running…",
    "done": "Done",
    "error": "Error",
    "input": "Input",
    "output": "Output"
  },
  "error": {
    "boundaryTitle": "Something went wrong here",
    "unknown": "Unknown rendering error"
  },
  "reasoning": {
    "thinking": "Thinking…",
    "redacted": "Hidden reasoning",
    "duration": "Thought for {{seconds}}s",
    "redactedMessage": "This reasoning block is encrypted (redacted) and cannot be viewed",
    "default": "Thinking"
  },
  "codeBlock": {
    "copy": "Copy code"
  },
  "mainChat": {
    "conversationTitle": "Chat with Workhorse",
    "placeholder": "Enter a task and I'll help you complete it",
    "aiDisclaimer": "Content generated by AI"
  }
}
```

- [ ] **Step 2: 验证 JSON 有效**

Run: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en-US.json','utf8')); console.log('valid')"`
Expected: `valid`

- [ ] **Step 3: 验证 TypeScript 编译通过**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 4: 提交基础设施**

```bash
git add src/i18n/ src/main.tsx package.json package-lock.json
git commit -m "feat(i18n): add i18next infrastructure with zh-CN and en-US locales"
```

---

## Task 4: 改造 ErrorBoundary

**Files:**
- Modify: `src/components/ErrorBoundary.tsx`

ErrorBoundary 是 class 组件，不能使用 hooks。需要用 `i18n.t()` 直接调用。

- [ ] **Step 1: 改造 ErrorBoundary**

在文件顶部添加 i18n 导入，替换硬编码字符串：

```typescript
// src/components/ErrorBoundary.tsx — 在第 1 行 import 后新增
import { Component, type ErrorInfo, type ReactNode } from 'react';
import i18n from '../i18n';   // ← 新增
```

在 `render()` 方法中替换三处硬编码字符串：

```tsx
// 替换 "这个区域出了点问题"
<p className="text-[13px] font-semibold text-gray-900 dark:text-gray-100 mb-1">
  {i18n.t('error.boundaryTitle')}
</p>

// 替换 '未知渲染错误'
<p className="text-[11.5px] text-gray-500 dark:text-gray-400 mb-4 break-all">
  {this.state.error?.message ?? i18n.t('error.unknown')}
</p>

// 替换 "重试"
<button
  type="button"
  onClick={this.handleRetry}
  className="px-3.5 py-1.5 rounded-lg bg-gray-800 dark:bg-gray-200 text-white dark:text-gray-800 text-[12px] font-medium hover:bg-gray-700 dark:hover:bg-gray-300 transition-colors"
>
  {i18n.t('common.retry')}
</button>
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/ErrorBoundary.tsx
git commit -m "feat(i18n): convert ErrorBoundary to use i18n"
```

---

## Task 5: 改造 ReasoningPart

**Files:**
- Modify: `src/components/chat/ReasoningPart.tsx`

- [ ] **Step 1: 添加 useTranslation hook 并替换硬编码字符串**

```typescript
// 在文件顶部 imports 中新增
import { useTranslation } from 'react-i18next';
```

在函数组件内添加 hook 调用：

```typescript
export default function ReasoningPart({
  text, status, redacted = false, startedAt, endedAt, autoExpand,
}: ReasoningPartProps) {
  const { t } = useTranslation();   // ← 新增
  const [open, setOpen] = useState(false);
  // ... 其余不变
```

替换 `label` 计算逻辑（约第 59-65 行）：

```typescript
  const label = streaming
    ? t('reasoning.thinking')
    : redacted
      ? t('reasoning.redacted')
      : durationSec != null
        ? t('reasoning.duration', { seconds: durationSec })
        : t('reasoning.default');
```

替换 redacted 消息（约第 85 行）：

```tsx
<span>{t('reasoning.redactedMessage')}</span>
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/chat/ReasoningPart.tsx
git commit -m "feat(i18n): convert ReasoningPart to use i18n"
```

---

## Task 6: 改造 CodeBlock

**Files:**
- Modify: `src/components/chat/CodeBlock.tsx`

- [ ] **Step 1: 替换 aria-label**

```typescript
// 在文件顶部 imports 中新增
import { useTranslation } from 'react-i18next';
```

在函数组件内添加 hook 并替换：

```typescript
export default function CodeBlock({ language, code, streaming = false }: CodeBlockProps) {
  const { t } = useTranslation();   // ← 新增
  // ... 其余不变
```

```tsx
// 替换 aria-label="复制代码"（约第 100 行）
<button
  type="button"
  data-slot="markdown-code-actions"
  onClick={handleCopy}
  className="..."
  aria-label={t('codeBlock.copy')}
>
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/chat/CodeBlock.tsx
git commit -m "feat(i18n): convert CodeBlock to use i18n"
```

---

## Task 7: 改造 ToolCallBlock

**Files:**
- Modify: `src/components/chat/ToolCallBlock.tsx`

- [ ] **Step 1: 替换状态标签和输入/输出标签**

```typescript
// 在文件顶部 imports 中新增
import { useTranslation } from 'react-i18next';
```

在函数组件内添加 hook。将硬编码的 `STATUS_LABEL` 替换为函数调用：

```typescript
// 删除原来的 STATUS_LABEL 常量（第 25-29 行）
// 改为在组件内使用 t()

export default function ToolCallBlock({ tool }: { tool: ToolCallData }) {
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);

  const statusLabel: Record<ToolCallData['status'], string> = {
    running: t('toolCall.running'),
    done: t('toolCall.done'),
    error: t('toolCall.error'),
  };

  // ...
```

在 JSX 中使用 `statusLabel`：

```tsx
<span className="text-gray-400 dark:text-gray-500 text-[10.5px]">
  {statusLabel[tool.status]}
</span>
```

替换"输入"/"输出"标签（约第 58、64 行）：

```tsx
<p className="text-[10px] text-gray-400 dark:text-gray-500 mb-0.5">{t('toolCall.input')}</p>
// ...
<p className="text-[10px] text-gray-400 dark:text-gray-500 mb-0.5">{t('toolCall.output')}</p>
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/chat/ToolCallBlock.tsx
git commit -m "feat(i18n): convert ToolCallBlock to use i18n"
```

---

## Task 8: 改造 TaskListModal

**Files:**
- Modify: `src/components/TaskListModal.tsx`

- [ ] **Step 1: 替换所有硬编码字符串**

```typescript
// 在文件顶部 imports 中新增
import { useTranslation } from 'react-i18next';
```

在函数组件内添加 hook 并替换：

```tsx
export default function TaskListModal({ onClose, onSelect }: TaskListModalProps) {
  const { t } = useTranslation();
  // ...

  // "任务列表" → t('tasks.title')
  <span className="text-[13px] font-semibold text-gray-900 dark:text-gray-100">{t('tasks.title')}</span>

  // aria-label="关闭任务列表" → aria-label={t('tasks.closeTaskList')}
  <button ... aria-label={t('tasks.closeTaskList')}>

  // placeholder="搜索任务…" → placeholder={t('tasks.searchPlaceholder')}

  // "无匹配任务" → t('tasks.noMatch')
  <p className="...">{t('tasks.noMatch')}</p>
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/TaskListModal.tsx
git commit -m "feat(i18n): convert TaskListModal to use i18n"
```

---

## Task 9: 改造 SettingsModal（含新增语言选择器）

**Files:**
- Modify: `src/components/SettingsModal.tsx`

这是最复杂的组件，需要：改造所有硬编码字符串 + 将 NavItem 类型从中文改为 key + 新增语言选择器。

- [ ] **Step 1: 重构 NavItem 类型和导航数据**

```typescript
// 替换原来的 type NavItem = '主题' | '快捷键' | 'Agent';
type NavItem = 'theme' | 'shortcuts' | 'agent';

// 替换原来的 SHORTCUTS 数组
const SHORTCUTS: { key: string; descKey: string }[] = [
  { key: '⌘ N',     descKey: 'shortcuts.newTask' },
  { key: '⌘ K',     descKey: 'shortcuts.globalSearch' },
  { key: '⌘ ,',     descKey: 'shortcuts.openSettings' },
  { key: '⌘ W',     descKey: 'shortcuts.closePanel' },
  { key: '⌘ \\',    descKey: 'shortcuts.toggleSidebar' },
  { key: '⌘ T',     descKey: 'shortcuts.newTerminal' },
  { key: '⌘ D',     descKey: 'shortcuts.splitTerminal' },
  { key: '⌘ Enter', descKey: 'shortcuts.sendMessage' },
  { key: '⌘ /',     descKey: 'shortcuts.showShortcuts' },
  { key: 'Esc',     descKey: 'shortcuts.cancelClose' },
];

// 替换原来的 STATUS_LABEL
// 删除: const STATUS_LABEL: Record<AgentConnection['status'], string> = { ... };
// 改为在组件内动态生成
```

- [ ] **Step 2: 改造 SettingsModal 主组件**

添加 hook：

```typescript
export default function SettingsModal({ onClose }: SettingsModalProps) {
  const { t } = useTranslation();
  // ...
  const [activeNav, setActiveNav] = useState<NavItem>('theme');
  const navLabels: Record<NavItem, string> = {
    theme: t('settings.nav.theme'),
    shortcuts: t('settings.nav.shortcuts'),
    agent: t('settings.nav.agent'),
  };
```

在 JSX 中替换：

```tsx
// Header: "设置" → t('settings.title')
<span className="text-[13.5px] font-semibold text-gray-900 dark:text-gray-100">{t('settings.title')}</span>

// Close button: aria-label="关闭设置" → aria-label={t('settings.closeSettings')}
<button ... aria-label={t('settings.closeSettings')}>

// Nav items: {(['主题', '快捷键', 'Agent'] as NavItem[])} →
{(['theme', 'shortcuts', 'agent'] as NavItem[]).map((item) => (
  <button ...>
    {navLabels[item]}
  </button>
))}

// Content sections:
{activeNav === 'theme' && <ThemeSection ... />}
{activeNav === 'shortcuts' && <ShortcutsSection />}
{activeNav === 'agent' && <AgentSection ... />}
```

- [ ] **Step 3: 改造 AgentSection**

在组件内添加 hook 并替换所有字符串：

```typescript
function AgentSection({ agent, autoExpandReasoning, setAutoExpandReasoning }: { ... }) {
  const { t } = useTranslation();

  const statusLabel: Record<AgentConnection['status'], string> = {
    idle: t('agent.status.disconnected'),
    connecting: t('agent.status.connecting'),
    connected: t('agent.status.connected'),
    error: t('agent.status.failed'),
  };

  // "连接" → t('settings.connection')
  <p className="...">{t('settings.connection')}</p>

  // status 显示 → {statusLabel[agent.status]}
  <span className="...">{statusLabel[agent.status]}</span>

  // "服务地址" → t('settings.endpoint')
  <label className="...">{t('settings.endpoint')}</label>

  // "默认值" → t('settings.defaultValue')
  <span className="...">{t('settings.defaultValue')}</span>

  // endpoint hint → t('settings.endpointHint')
  <p className="...">{t('settings.endpointHint')}</p>

  // "断开连接" → t('settings.disconnect')
  // "连接中…" → t('agent.status.connecting')
  // "重新连接" → t('settings.reconnect')
  {isConnecting ? t('agent.status.connecting') : t('settings.reconnect')}

  // "推理显示" → t('settings.reasoningDisplay')
  <p className="...">{t('settings.reasoningDisplay')}</p>

  // "自动展开思考过程" → t('settings.autoExpandThinking')
  // "流式推理时自动展开，完成后自动折叠" → t('settings.autoExpandDescription')
```

- [ ] **Step 4: 改造 ThemeSection — 新增语言选择器**

```typescript
function ThemeSection({ isDarkMode, setIsDarkMode }: { ... }) {
  const { t, i18n } = useTranslation();

  return (
    <div>
      <p className="text-[11.5px] font-semibold text-gray-400 dark:text-gray-500 tracking-wider mb-4">
        {t('settings.appearance')}
      </p>
      <div className="grid grid-cols-2 gap-3">
        <ThemeOption
          label={t('settings.lightTheme')}
          icon={<Sun className="w-5 h-5" />}
          active={!isDarkMode}
          onClick={() => setIsDarkMode(false)}
        />
        <ThemeOption
          label={t('settings.darkTheme')}
          icon={<Moon className="w-5 h-5" />}
          active={isDarkMode}
          onClick={() => setIsDarkMode(true)}
        />
      </div>

      {/* 语言选择器 — 新增 */}
      <div className="mt-4 pt-4 border-t border-outline/40 dark:border-neutral-800/40">
        <label className="block text-[11px] text-gray-400 dark:text-gray-500 mb-1.5">
          {t('settings.language')}
        </label>
        <select
          value={i18n.language}
          onChange={(e) => i18n.changeLanguage(e.target.value)}
          className="w-full px-3 py-2 rounded-lg bg-gray-50 dark:bg-neutral-800/60 border border-outline/40 dark:border-neutral-800/50 text-[12.5px] text-gray-700 dark:text-gray-300 outline-none focus:ring-1 focus:ring-gray-300 dark:focus:ring-neutral-700"
        >
          <option value="zh-CN">中文 (简体)</option>
          <option value="en-US">English</option>
        </select>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: 改造 ShortcutsSection**

```typescript
function ShortcutsSection() {
  const { t } = useTranslation();

  return (
    <div>
      <p className="text-[11.5px] font-semibold text-gray-400 dark:text-gray-500 tracking-wider mb-4">
        {t('settings.keyboardShortcuts')}
      </p>
      <div className="grid grid-cols-2 gap-x-8 gap-y-0">
        {SHORTCUTS.map(({ key, descKey }) => (
          <div key={key} className="...">
            <span className="...">{t(descKey)}</span>
            <kbd className="...">{key}</kbd>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 6: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add src/components/SettingsModal.tsx
git commit -m "feat(i18n): convert SettingsModal to i18n with language selector"
```

---

## Task 10: 改造 RightPanel

**Files:**
- Modify: `src/components/RightPanel.tsx`

- [ ] **Step 1: 重构 Tab 类型并替换硬编码字符串**

```typescript
// 替换: type Tab = '目录' | '信息' | '预览';
type Tab = 'directory' | 'info' | 'preview';
```

```typescript
import { useTranslation } from 'react-i18next';

export default function RightPanel({ onClose }: RightPanelProps) {
  const { t } = useTranslation();
  const [activeTab, setActiveTab] = useState<Tab>('directory');

  const tabs: { key: Tab; label: string }[] = [
    { key: 'directory', label: t('workspace.tabs.directory') },
    { key: 'info', label: t('workspace.tabs.info') },
    { key: 'preview', label: t('workspace.tabs.preview') },
  ];

  // JSX 中替换:
  // title="收起工作台" → title={t('workspace.collapsePanel')}
  // {tab} → {tab.label}  (用 tab.key 作为 setActiveTab 的参数)
  // onClick={() => setActiveTab(tab)} → onClick={() => setActiveTab(tab.key)}
  // activeTab === '目录' → activeTab === 'directory'
  // activeTab === '信息' → activeTab === 'info'
  // activeTab === '预览' → activeTab === 'preview'
```

替换 info 标签区域：

```tsx
// "访问 ID" → t('workspace.fields.accessId')
// "标签" → t('workspace.fields.tags')
// "创建时间" → t('workspace.fields.createdAt')
// "分支" → t('workspace.fields.branch')
// "原始指令" → t('workspace.fields.originalCommand')
```

替换 preview 按钮：

```tsx
// "打开" → t('workspace.actions.open')
// "所在位置" → t('workspace.actions.revealInFolder')
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/RightPanel.tsx
git commit -m "feat(i18n): convert RightPanel to use i18n"
```

---

## Task 11: 改造 AgentRail

**Files:**
- Modify: `src/components/AgentRail.tsx`

这是最复杂的组件，约 25 处硬编码字符串。

- [ ] **Step 1: 添加 hook 并替换连接状态**

```typescript
import { useTranslation } from 'react-i18next';
```

替换 `AGENT_STATUS_TITLE`（约第 169-174 行）。删除常量，改为在组件内用 hook：

```typescript
// 删除 AGENT_STATUS_TITLE 常量
// 在 AgentRail 函数组件内使用 t()
export default function AgentRail() {
  const { t } = useTranslation();
  // ...

  // 构建状态标题函数
  const statusTitle = (a: AgentConnection): string => {
    switch (a.status) {
      case 'idle': return t('agent.status.disconnected');
      case 'connecting': return t('agent.status.connecting');
      case 'connected': return t('agent.status.connected', { sessionId: a.sessionId });
      case 'error': return t('agent.status.failed', { error: a.error ?? '' });
    }
  };
```

- [ ] **Step 2: 替换 PendingStar 的 aria-label**

```tsx
// PendingStar 不用 hook（无组件上下文），用 i18n.t() 直接调用
import i18n from '../i18n';

function PendingStar() {
  return (
    <span className="inline-flex items-center" aria-label={i18n.t('agent.generating')}>
      <Sparkles className="w-4 h-4 agent-pending-star" />
    </span>
  );
}
```

- [ ] **Step 3: 替换 PermissionCard 中的字符串**

PermissionCard 是独立函数组件，添加 `useTranslation`：

```typescript
function PermissionCard({ part, onDecide }: { ... }) {
  const { t } = useTranslation();
  // ...

  // "请求权限：{tool}" → t('agent.permissionRequest') + tool (注意 tool 不在翻译中，拼接)
  <span>{t('agent.permissionRequest')}{tool}</span>

  // "敏感操作" → t('agent.sensitiveAction')
  <span className="...">{t('agent.sensitiveAction')}</span>

  // "允许" → t('common.allow')
  // "拒绝" → t('common.deny')
  // "已允许（本次会话记住）" → t('agent.allowedRemembered')
  // "已拒绝" → t('agent.denied')
  <span>{status === 'allowed' ? t('agent.allowedRemembered') : t('agent.denied')}</span>
```

- [ ] **Step 4: 替换输入区和欢迎语**

```tsx
// placeholder="请输入任务，交给我来完成"
placeholder={t('agent.placeholder')}

// "选择文件"
<span>{t('agent.chooseFile')}</span>

// aria-label="停止" → aria-label={t('common.stop')}
// aria-label="发送" → aria-label={t('common.send')}

// "有什么可以帮你的？"
<p className="...">{t('agent.welcome')}</p>

// 反馈按钮 title
<button ... title={t('agent.feedback.copy')}><Copy .../></button>
<button ... title={t('agent.feedback.good')}><ThumbsUp .../></button>
<button ... title={t('agent.feedback.bad')}><ThumbsDown .../></button>

// aria-label="滚到底部" → aria-label={t('agent.scrollToBottom')}

// Footer 按钮
// title="任务列表" → title={t('tasks.title')}
// aria-label="打开任务列表" → aria-label={t('agent.openTaskList')}
// title="设置" → title={t('settings.title')}
// aria-label="打开设置" → aria-label={t('agent.openSettings')}

// AGENT_STATUS_TITLE[agent.status](agent) → statusTitle(agent)
<span ... title={statusTitle(agent)} />
```

- [ ] **Step 5: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src/components/AgentRail.tsx
git commit -m "feat(i18n): convert AgentRail to use i18n"
```

---

## Task 12: 改造 Terminal 组件

**Files:**
- Modify: `src/components/terminal/TerminalWorkspace.tsx`
- Modify: `src/components/terminal/TabBar.tsx`
- Modify: `src/components/terminal/ProfileMenu.tsx`

- [ ] **Step 1: 改造 TerminalWorkspace**

```typescript
import { useTranslation } from 'react-i18next';

export default function TerminalWorkspace() {
  const { t } = useTranslation();
  // ...

  // "暂无终端" → t('terminal.noTerminal')
  <p className="text-[13px]">{t('terminal.noTerminal')}</p>

  // label="新建终端" → label={t('terminal.newTerminal')}
  <ProfileMenu label={t('terminal.newTerminal')} onSelect={addGroup} />
```

- [ ] **Step 2: 改造 TabBar**

```typescript
import { useTranslation } from 'react-i18next';

export default function TabBar({ ... }: TabBarProps) {
  const { t } = useTranslation();

  // aria-label="关闭终端组" → aria-label={t('terminal.closeGroup')}
  // title="关闭" → title={t('terminal.close')}
  // title="新建终端组" → title={t('terminal.newGroup')}
  <ProfileMenu onSelect={onAddGroup} title={t('terminal.newGroup')} />
```

- [ ] **Step 3: 改造 ProfileMenu**

ProfileMenu 的 `label` 和 `title` 已经是从父组件传入的 props，不需要改造。只有 `title` 的默认值需要改：

```typescript
// title={title ?? '新建终端'} → title={title ?? t('terminal.newTerminal')}
// 但 ProfileMenu 本身不持有翻译上下文，title 由父组件传入已翻译值即可
// 默认值保持英文 key fallback 不影响，因为所有调用方都显式传了 title/label
```

ProfileMenu 无需修改，因为所有硬编码中文都由父组件传入。

- [ ] **Step 4: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/components/terminal/TerminalWorkspace.tsx src/components/terminal/TabBar.tsx
git commit -m "feat(i18n): convert Terminal components to use i18n"
```

---

## Task 13: 改造 MainChat（Mock 组件）

**Files:**
- Modify: `src/components/MainChat.tsx`

注意：MainChat 当前是 mock/demo 组件（非活跃使用），但仍需统一改造。

- [ ] **Step 1: 替换硬编码字符串**

```typescript
import { useTranslation } from 'react-i18next';

export default function MainChat() {
  const { t } = useTranslation();

  // "与Workhorse的对话" → t('mainChat.conversationTitle')
  // placeholder="请输入任务，交给我来帮你完成" → placeholder={t('mainChat.placeholder')}
  // "选择文件" → t('agent.chooseFile')
  // "以上内容由 AI 生成" → t('mainChat.aiDisclaimer')

  // Mock 对话内容中的中文（论文分析文本）不需要翻译 — 这是示例数据
  // title 属性: "复制内容" / "觉得好用" / "觉得不好" 使用 t()
  <button ... title={t('agent.feedback.copy')}><Copy .../></button>
  <button ... title={t('agent.feedback.good')}><ThumbsUp .../></button>
  <button ... title={t('agent.feedback.bad')}><ThumbsDown .../></button>
```

- [ ] **Step 2: 验证编译**

Run: `npm run lint`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add src/components/MainChat.tsx
git commit -m "feat(i18n): convert MainChat to use i18n"
```

---

## Task 14: 最终验证 + 清理

**Files:**
- 无新增文件

- [ ] **Step 1: 全量 TypeScript 编译检查**

Run: `npm run lint`
Expected: PASS，无 warning

- [ ] **Step 2: 扫描残留硬编码中文字符串**

Run: `grep -rn '[一-鿿]' src/components/ src/chat/ --include='*.tsx' | grep -v 'node_modules' | grep -v '.mock.' | head -40`

Expected: 仅允许以下例外：
- MainChat.tsx 中的 mock 对话文本（示例数据，非 UI 文案）
- RightPanel.tsx 中的 mock 数据标签（如 `GPT-4o 系统卡深度分析报告`，非 UI 文案）
- 文件路径中的中文（如 `C:\\Users\\wushengzhou`）

如果有遗漏的 UI 文案硬编码，补充提取到翻译文件。

- [ ] **Step 3: 启动渲染器验证**

Run: `npm run dev`

手动验证：
1. 默认中文显示正常
2. 打开设置 → 外观 → 语言切换到 English
3. 所有 UI 文案切换为英文
4. 刷新页面，语言保持 English（localStorage 持久化）
5. 切回中文，一切正常

- [ ] **Step 4: 更新 AGENTS.md 文档**

在 AGENTS.md 的 Hard Rules 附近或 Knowledge Map 中添加 i18n 参考：

```markdown
### i18n
- [`src/i18n/`](./src/i18n/) — react-i18next 配置与翻译文件。
- 新增 UI 文案时，先在 `src/i18n/locales/zh-CN.json` 添加 key，
  再在 `en-US.json` 添加对应翻译，组件中使用 `useTranslation` hook。
```

- [ ] **Step 5: 最终提交**

```bash
git add -A
git commit -m "feat(i18n): complete i18n implementation for zh-CN and en-US

- Add react-i18next with i18next-browser-languagedetector
- Create zh-CN.json and en-US.json translation files (~90 keys)
- Convert all 21 components to use useTranslation hook
- Add language selector in Settings → Appearance
- Language persists via localStorage, defaults to system language
- Update AGENTS.md with i18n guidance"
```

---

## 自审查 Checklist

- [x] **Spec 覆盖**：每个需求都有对应 Task（基础设施 Task 1-3，组件改造 Task 4-13，验证 Task 14）
- [x] **无占位符**：所有 Step 包含完整代码，无 TBD/TODO
- [x] **类型一致**：NavItem 类型从中文改为英文 key，Tab 类型同步修改，所有 t() key 与 JSON 定义对应
- [x] **遗漏检查**：TitleBar 组件无需改造（标签已是英文 "Minimize"/"Maximize"/"Close"），已在扫描中确认
